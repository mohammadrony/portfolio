# Release Upgrade

Update package

```sh
sudo apt update
sudo apt upgrade -y
```

Upgrade operating system

```sh
sudo do-release-upgrade
```

Update all held back package

```sh
sudo apt update
sudo apt full-upgrade -y
```

Restart

```sh
sudo reboot now
```
