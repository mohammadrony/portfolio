# Root Shell

- Enter into GRUB menu with `ESC` key
- Start with Advanced optioins for Ubuntu
- Select Ubuntu with recovery mode
- Enter with 'root - Drop to root shell prompt'

Remount filesystem in read write mode

```sh
mount -o remount,rw /
```
