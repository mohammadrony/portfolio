# Crictl

List containers

```bash
sudo crictl ps
```

Stop container

```bash
sudo crictl stop <crontainer_id>
```

Pull image

```bash
sudo crictl pull <image>
```

Logs

```bash
crictl logs <container_id>
```

Inspect

```bash
crictl inspect <container_id>
```
