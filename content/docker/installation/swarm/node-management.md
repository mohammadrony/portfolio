# Node Management

Node list

```bash
docker node ls
```

Promote to manager node

```bash
docker node promote <node-name>
```

Demote to worker node

```bash
docker node demote <node-name>
```

Remove node

```bash
docker node rm <node-name>
```

```bash
docker node rm -f <node-name>
```
