apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: blue-green-app
  namespace: argocd
spec:
  destination:
    namespace: blue-green
    server: "https://kubernetes.default.svc"
  project: default
  source:
    path: configuration/argo/blue-green
    repoURL: "https://gitlab.com/mohammadrony/kubernetes.git"
    targetRevision: main
    directory:
      recurse: true
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
