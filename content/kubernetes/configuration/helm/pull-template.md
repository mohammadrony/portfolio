# Pull Template

## Download Repo

Save chart in archive file

```bash
helm pull <chart>
```

Extract chart files

```bash
helm pull <chart> --untar
```

## Generate Template

```bash
helm template <name> <chart> > myfile.yaml
```
